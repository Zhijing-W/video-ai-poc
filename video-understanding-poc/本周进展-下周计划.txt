本周进展（2026.06.12 - 2026.06.18）：
- 完成 Phase 2（成本可控混合架构）完整实现：YOLOv8 + 事件门控 + 巡航裁决，LLM 成本节省 80%+
- 实现双模式 UI：监控模式（实时仪表盘）+ 技术模式（可视化架构流程图）
- 支持实时监控（摄像头 + 本地视频）、比对功能（文字/图片）、结构化 JSON 输出
- 核心功能：6 个 API 端点、完整前后端、Docker 部署
- 代码重构 Phase 1（CSS 提取）完成，为 AI 快速迭代打基础

下周目标（2026.06.19 - 2026.06.25）：
- 完成 Phase 3 核心实现：从"逐帧看图"升级到"逐轨迹认人 + 主体记忆"
- 核心任务 1：集成 MOT 多目标跟踪（ByteTrack/BoT-SORT），给每个目标稳定 track_id，实现跨帧关联
- 核心任务 2：构建分阶段识别流程：YOLO 检测 → 事件门控 → LLM 语义理解 → ReID 跨帧关联
- 核心任务 3：实现 ReID 向量库（主体记忆）：识别一次后记住，同一主体再出现直接复用档案，不再调 LLM
- 核心任务 4：接入细粒度感知（Pose/Human Parsing），修复 Phase 2 颜色/属性误判问题
- 核心任务 5：优化 LLM 使用策略：分层处理 + 筛选机制，送 crop 不送整帧，事件驱动触发

验收标准：
1. 完整链路可运行：视频输入 → 抽帧 → MOT 跟踪 → 多阶段推理 → ReID 关联 → 输出结果
2. MOT 跟踪稳定分配 track_id，同一目标跨帧保持 ID 一致
3. ReID 向量库工作：同一主体第二次出现能识别并复用档案，不重复调用 LLM
4. 细粒度感知修复颜色判断（Pose 定位部件区 + Human Parsing 取色）
5. LLM 调用进一步降低：仅在新主体/灰区/报警条件触发，送 crop 而非整帧
6. 量化收益：单位视频 LLM 调用次数相比 Phase 2 再降 50%+，识别准确率不降反升

当前状态与原计划差距：
- ✅ Phase 1（LLM-first MVP）已完成
- ✅ Phase 2（成本可控混合架构）已完成
- ⏳ Phase 3（逐轨迹识别与主体记忆）下周实施：MOT 跟踪 + ReID 向量库 + 细粒度感知 + 分层推理
- 🎯 Phase 4（Azure 全链路上云）之后进行
