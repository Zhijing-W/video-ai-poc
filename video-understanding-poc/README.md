# Video Understanding PoC - Monitor v1

本分支保存冻结的旧版实时监控应用。

代码入口：

```text
video-understanding-poc/monitor-v1/
```

运行前阅读 `monitor-v1/README.md` 和 `monitor-v1/CODE_MAP.md`。

`.venv`、`.env`、数据、输出和模型权重不提交到 Git。
