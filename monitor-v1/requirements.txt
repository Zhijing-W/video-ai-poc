# Phase 1 (LLM-first MVP) dependencies
# Web 层（后续 FastAPI 阶段用，竖切脚本不强依赖）
fastapi
uvicorn
gunicorn
python-multipart
jinja2

# Azure
azure-storage-blob
azure-identity
openai>=1.40.0

# 工具
pydantic>=2.0
python-dotenv

# 抽帧兜底：本地已装 ffmpeg 时不需要；部署到没有 ffmpeg 的环境时启用，
# imageio-ffmpeg 自带一份静态 ffmpeg 二进制，可免 Docker。
imageio-ffmpeg

# Phase 2 · Step 5：YOLO 物体检测层（廉价守门员，给事件门控提供依据）。
# ultralytics 会按平台拉取 CPU 版 torch；首次调用自动下载模型权重。
ultralytics

# Phase 3 · Step 11 / Phase 4：多目标跟踪（MOT）。
# 复用 ultralytics 内置 BYTETracker / BOTSORT；BoT-SORT+ReID 的外观特征复用 app/reid.py。
# 线性分配用 lap，Phase 4 起改用其维护分叉 lapx
# （lapx 由下方 boxmot 带入，drop-in 兼容 `import lap`；不要再单独装 lap，二者会冲突覆盖）。

# Phase 3 · Step 14：主体记忆 / ReID 向量库。
# faiss-cpu：向量库余弦检索（IndexIDMap2 + IndexFlatIP）。
# torchvision：ReID 兜底后端（ResNet50 ImageNet 通用骨干，2048 维；非专用 ReID）。
faiss-cpu
torchvision

# Phase 4：ReID 升级为真·行人重识别 OSNet（域泛化）。
# boxmot：提供 OSNet/OSNet-AIN 等预训练 ReID（默认 osnet_ain_x1_0_msmt17，512 维，权重首次自动下载）；
#   同时带入 lapx（替代 lap）。比通用 resnet50 强一个量级，REID_BACKEND=auto 会优先选 osnet。
boxmot

# Phase 4 · Step 20：人脸识别分支。
# insightface：SCRFD 检测 + ArcFace(w600k_r50) 识别，512 维归一化 embedding（业界标杆，与客户 UniFace 同源）。
# onnxruntime：insightface 的推理后端（CPU）。首次运行自动下 ~300MB buffalo_l 模型到 ~/.insightface。
insightface
onnxruntime

# Phase 4 · Step 27b：攻"人脸模糊"的进阶武器（可插拔，默认开，测试可关）。
# gfpgan：人脸超分（识别前把糊脸拉清），首次自动下 ~330MB GFPGANv1.3 权重。
#   注意：其依赖 basicsr 引用了新版 torchvision 已删除的 functional_tensor —— app/face.py 内置
#   运行时 monkeypatch（_patch_basicsr）兜底，无需手改 venv。
# 3D-68 几何 cue 复用 insightface buffalo_l 自带的 1k3d68（无需额外装）。
gfpgan

# Phase 4 · Step 29：场景文字 OCR（LANE D 子能力，默认关，--ocr / OCR_ENABLED 开）。
# rapidocr_onnxruntime：内置 PP-OCRv4 检测+识别（与 PaddleOCR 同源模型），走已装的 onnxruntime，
#   CPU 友好、Windows 安装干净、~15MB 模型自带。读时间戳/车牌/单号→scene_context 喂 LLM（不进 subject_id）。
#   可插拔：OCR_BACKEND=paddleocr 切 PaddleOCR（需另装 paddlepaddle+paddleocr，上云追更高精度）。
rapidocr_onnxruntime

# 仅文档用（生成架构图，App 运行时不需要，部署可不装）：
#   pip install matplotlib
#   python scripts/make_arch_diagram.py
